services:
  redis:
    image: redis/redis-stack-server:latest
    container_name: youtube-rag-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data: